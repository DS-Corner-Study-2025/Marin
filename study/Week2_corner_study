📌 빈칸 채우기
1. ( )는 기존의 스프링 프레임워크를 기반으로 스프링의 모든 기능을 제공하면서 스프링 설정을 자동화한 프레임워크다.
👉 정답: 스프링 부트

2. Src/main/resources/static 폴더에 hello.html 파일을 생성했다면, 결과를 확인하기 위해 접속해야 하는 URL은?
👉 정답: http://localhost:8080/hello.html

3. 도메인 객체는 ( )와 ( ) 메서드를 추가해야 한다.
👉 정답: Setter / Getter

4. 데이터베이스나 파일에 접근하여 데이터를 처리하는 곳으로 데이터 액세스 계층인 곳은?
👉 정답: 퍼시스턴스 계층

5. 애플리케이션이 제공하는 포괄적인 서비스를 표현하는 계층은?
👉 정답: 비즈니스 계층






📌 ###########코드1
// 서비스 계층 코드
package com.springboot.service;

import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

import com.springboot.domain.Book;
import com.springboot.repository.BookRepository;

// @Service : 이 클래스가 서비스 계층임을 나타냄
@Service
public class BookServiceImpl implements BookService {

    // BookRepository를 자동 주입 받아 사용 (퍼시스턴스 계층 연결)
    @Autowired
    private BookRepository bookRepository;

    @Override
    public List<Book> getAllBookList() {
        // 저장소(BookRepository)에서 모든 책 데이터를 가져오는 메서드 호출
        return bookRepository.getAllBookList();
    }
}














📌 ##################코드2
// 컨트롤러 계층 코드
package com.springboot.controller;

import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;

import com.springboot.domain.Book;
import com.springboot.service.BookService;

// @Controller : 웹 요청을 처리하는 컨트롤러 클래스임을 명시
@Controller
public class BookController {

    // BookService를 자동 주입 받아 사용 (비즈니스 계층 연결)
    @Autowired
    private BookService bookService;

    // @RequestMapping : "/books" 경로로 GET 요청이 들어오면 실행
    @RequestMapping(value = "/books", method = RequestMethod.GET)
    public String requestBookList(Model model) {

        // 서비스 계층(BookService)을 통해 모든 책 데이터 가져오기
        List<Book> list = bookService.getAllBookList();

        // 뷰에 "bookList"라는 이름으로 데이터 전달
        model.addAttribute("bookList", list);

        // "books"라는 뷰 페이지로 이동 (books.jsp or books.html)
        return "books";
    }
}
